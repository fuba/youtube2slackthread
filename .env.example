# Slack Configuration (Required)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_SIGNING_SECRET=your-signing-secret-here

# Server Configuration
PORT=3000
CONFIG_PATH=config.yaml

# Optional Slack Settings
SLACK_DEFAULT_CHANNEL=general
SLACK_APP_TOKEN=xapp-1-your-app-token-here  # Required for Socket Mode (file uploads)

# User Cookie Management
COOKIE_ENCRYPTION_KEY=your-secure-encryption-key-here  # Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"